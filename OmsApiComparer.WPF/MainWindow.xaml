<Window x:Class="OmsApiComparer.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OmsApiComparer.WPF"

        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ListBox ItemsSource="{Binding Requests}" DisplayMemberPath="MethodAndPath"
                 SelectedItem="{Binding SelectedRequest.Value}" />
        
       <ScrollViewer Grid.Column="1">
            <StackPanel>
                <Expander Header="Query string parameters" IsExpanded="True">
                    <DataGrid ItemsSource="{Binding SelectedRequest.Value.QueryString}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="*" Header="Property Name" Binding="{Binding A.Name}" />
                            <DataGridTextColumn Width="*" Header="Tobacco" Binding="{Binding A.Value}" />
                            <DataGridTextColumn Width="*" Header="NCP" Binding="{Binding B.Value}" />
                            <DataGridTextColumn Width="*" Header="OTP" Binding="{Binding C.Value}" />
                            <DataGridTextColumn Width="*" Header="Milk" Binding="{Binding D.Value}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Expander>
                <Expander Header="Request headers" IsExpanded="True">
                    <DataGrid ItemsSource="{Binding SelectedRequest.Value.Headers}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="*" Header="Property Name" Binding="{Binding A.Name}" />
                            <DataGridTextColumn Width="*" Header="Tobacco" Binding="{Binding A.Value}" />
                            <DataGridTextColumn Width="*" Header="NCP" Binding="{Binding B.Value}" />
                            <DataGridTextColumn Width="*" Header="OTP" Binding="{Binding C.Value}" />
                            <DataGridTextColumn Width="*" Header="Milk" Binding="{Binding D.Value}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Expander>
                <Expander Header="Request objects" IsExpanded="True">
                    <ItemsControl ItemsSource="{Binding SelectedRequest.Value.RequestObjects}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:ObjectViewModel}">
                                <GroupBox Header="{Binding Name}">
                                    <DataGrid ItemsSource="{Binding Properties}" AutoGenerateColumns="False" IsReadOnly="True">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Width="*" Header="Property Name" Binding="{Binding A.Name}" />
                                            <DataGridTextColumn Width="*" Header="Tobacco" Binding="{Binding A.Value}" />
                                            <DataGridTextColumn Width="*" Header="NCP" Binding="{Binding B.Value}" />
                                            <DataGridTextColumn Width="*" Header="OTP" Binding="{Binding C.Value}" />
                                            <DataGridTextColumn Width="*" Header="Milk" Binding="{Binding D.Value}" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </GroupBox>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>
                <Expander Header="Responses" IsExpanded="True">
                    <ItemsControl ItemsSource="{Binding SelectedRequest.Value.ResponseObjects}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:ObjectViewModel}">
                                <GroupBox Header="{Binding Name}">
                                    <DataGrid ItemsSource="{Binding Properties}" AutoGenerateColumns="False" IsReadOnly="True">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Width="*" Header="Property Name" Binding="{Binding A.Name}" />
                                            <DataGridTextColumn Width="*" Header="Tobacco" Binding="{Binding A.Value}" />
                                            <DataGridTextColumn Width="*" Header="NCP" Binding="{Binding B.Value}" />
                                            <DataGridTextColumn Width="*" Header="OTP" Binding="{Binding C.Value}" />
                                            <DataGridTextColumn Width="*" Header="Milk" Binding="{Binding D.Value}" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </GroupBox>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Expander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
